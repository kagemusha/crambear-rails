#= require spec_helper
#= require store

describe "App.Store", ->
#  beforeEach (done)->
#    App.testStore = App.Store.create()
#    done()
#
#  afterEach -> App.testStore.destroy()
#
#  it "is a DS.Store", ->
#    assert.ok(App.Store)
#    assert.ok(DS.Store.detect(App.Store))
#
#  it "works with latest Ember-Data revision 11", ->
#    assert.equal(store.get('revision'), 11)

#  describe "adapter", ->
#    it "is a DS.RESTAdapter", ->
#      assert.ok(DS.RESTAdapter.detectInstance(store.get('adapter')))
#
#    it "has a namespace 'api'", ->
#      assert.ok(store.get('adapter.namespace'), 'api')

