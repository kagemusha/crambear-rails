li.set_label{bindAttr class="isEditing"}
  if isEditing
    button.mini{action delete} X
    | &nbsp;
    Em.TextField valueBinding="name" class="name"
    button.mini{action save} Save
  else
    = name
